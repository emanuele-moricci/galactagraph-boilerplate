import { {{ capital ModelName }} } from '@prisma/client';

import { {{#if wantQuery}}getAll{{ capitalPlural ModelName }}{{/if}}{{#if (and wantQuery wantMutation)}}, {{/if}}{{#if wantMutation}}create{{ capital ModelName }}{{/if}} } from '@src/services/{{ firstLower ModelName }}Service';

const resolver = {
  {{#if wantQuery}}
  Query: {
    {{ capital ModelName }}: async (_source, _args, _context, _info): Promise<{{ capital ModelName }}[]> => {
      return getAll{{ capitalPlural ModelName }}();
    },
  },
  {{/if}}
  {{#if wantMutation}}
  Mutation: {
    create{{ capital ModelName }}: {
      resolve: async (_, { input }): Promise<{{ capital ModelName }}> => {
        return create{{ capital ModelName }}(input);
      },
    },
  },
  {{/if}}
};

export default resolver;
