{{ parsedServiceName }}:
    build:
      context: './services/{{ parsedServiceName }}'
    ports:
      - '{{ ServicePort }}:{{ ServicePort }}'
    depends_on:
      - db
      - federation-gateway
    networks:
      - dev_vm
  # [ADD NEW DOCKER CONTAINERS HERE] <- DO NOT REMOVE - Needed for the generator to create micro-service commands seamlessly